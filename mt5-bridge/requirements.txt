Flask==2.3.3
requests==2.31.0
python-dotenv==1.0.0
apscheduler==3.10.4
pytz==2023.3
werkzeug==2.3.7
flask-cors==4.0.0
schedule==1.2.0
# Note: MetaTrader5 package is Windows-only and should be installed on the host system
# The bridge will communicate with MT5 via TCP/IP connection
